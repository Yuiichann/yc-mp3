import React from 'react';
import { StreamItem } from '../types';

interface Props {
  radioStream: StreamItem;
}

const AudioStreamItem = ({ radioStream }: Props) => {
  return <div>AudioStreamItem</div>;
};

export default AudioStreamItem;
