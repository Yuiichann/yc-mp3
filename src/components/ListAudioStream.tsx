import React from 'react';
import { StreamItem } from '../types';

interface Props {
  dataStream: StreamItem[];
}

const ListAudioStream = ({ dataStream }: Props) => {
  return <div>ListAudioStream</div>;
};

export default ListAudioStream;
